// Vitest setup — applied before every test file.
// Keep this minimal; per-file jsdom behavior is enabled via a
// `// @vitest-environment jsdom` comment at the top of tests that need it.

import '@testing-library/jest-dom/vitest';
